# 数独教练 iOS PWA v3.1

## 本次 OCR 修正
- 先通过四角进行透视校正，再固定切成 9×9 共 81 格。
- 每个格子先做图像分析：只有尺寸足够大、位置接近格子中央的主数字才进入 OCR。
- 小号候选/备注数字会被过滤并保持为空格。
- OCR 识别结果按原始 81 格坐标回填，不再把整张图片识别出的数字按顺序连续塞入棋盘。
- OCR 校对区改成真正的 9×9 / 3×3 宫格显示。
- 导入后直接进入“做题”页，并按 9×9 坐标生成棋盘。

## 测试版更新
将本目录文件覆盖上传到 GitHub 仓库的 `test/` 目录即可。
